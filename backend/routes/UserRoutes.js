
// import express from "express";
// import { saveUserProfile } from "../controllers/UserController.js"; // ✅ Import controller

// const router = express.Router();

// // POST /api/users/save-profile
// router.post("/save-profile", async (req, res) => {
//   try {
//     await saveUserProfile(req.body); // ✅ Call the controller function
//     res.status(200).json({ message: "User profile saved/exists" });
//   } catch (error) {
//     console.error(error);
//     res.status(500).json({ message: "Failed to save user profile" });
//   }
// });

// export default router;
